import ChatContainer from './ChatContainer';

/**
 * ChatPage - Standalone chat page component
 * Thin wrapper around ChatContainer for use as a full page
 */
export default function ChatPage() {
  return <ChatContainer />;
}
